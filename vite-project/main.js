import './style.css'

window.insert = function insert(num) {
    document.getElementById('resultado').innerHTML = num;
}

function insert(num) {
    document.getElementById('resultado').innerHTML = num;
}